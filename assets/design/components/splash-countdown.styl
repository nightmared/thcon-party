.splash-countdown
  display flex
  flex-direction column
  margin 0
  padding $gap-huge $gap-huge 0
  color $white
  font-size calc(min(6vw 5rem))
  text-align center

  > .countdown
    color alpha($white, 50%)
    font-size calc(min(4vw, 3rem))
